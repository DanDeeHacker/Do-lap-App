# Dependencies / build artifacts (Railway rebuilds these in the Docker image)
frontend/node_modules/
frontend/dist/
backend/.venv_posix/
**/__pycache__/
*.pyc

# Dev SQLite database — production uses a Railway volume (DOSSLAP_DB_PATH).
# Never commit real user/runner data or the seeded dev DB.
*.db
backend/dosslap.db

# Backtest / analysis artifacts (regenerated by build_backtest_*.py)
*.xlsx
~$*.xlsx

# Bundled Garmin source — installed from PyPI via requirements.txt
python-garminconnect-master/

# Local tooling / OS
.DS_Store
.vscode/
.claude/
_data/
backend/backtest_engine_compare.xlsx

# In-app annotation mirror for Claude Code (user notes) — backend/feedback_sync.py
feedback/

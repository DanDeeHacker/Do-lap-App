fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
# Postgres driver (psycopg v3). Used automatically when DATABASE_URL is set
# (e.g. a Railway Postgres) so account history survives redeploys with no
# volume; local dev + tests fall back to a SQLite file with no driver needed.
psycopg[binary]>=3.1
pydantic>=2.9
email-validator>=2.2
passlib[argon2]>=1.7.4
python-multipart>=0.0.9
pytest>=8.3
httpx>=0.27
python-dotenv>=1.0
# Direct Garmin Connect download (data.html → „Stáhnout data přímo z Garmin
# Connect"). The bundled python-garminconnect-master is authoritative — install
# it with:  pip install ../python-garminconnect-master
# These two are its runtime deps and are enough if you use the PyPI package
# instead (pip install garminconnect):
curl_cffi>=0.15.0
garminconnect>=0.2.28
# Encrypts the stored Garmin OAuth session token at rest (only when
# DOSSLAP_SECRET is set; without it the token is stored plaintext on the
# private volume). Never stores the account password.
cryptography>=42.0
# Excel export for the in-app v1-vs-v2 engine backtest download.
openpyxl>=3.1
